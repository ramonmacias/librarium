# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# We don't want to upload all the vendor folder
vendor/

# We don't want to upload the coverage generated by go
coverage.html